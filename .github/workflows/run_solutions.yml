name: Test Run Solutions
on: [push]
jobs:
    test_solutions:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
              with:
                  submodules: recursive
            - run: ./run_tests
